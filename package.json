{
	"name": "workers",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "vite",
		"build": "vite build",
		"build:pages": "./build.sh",
		"build:main": "echo '🚀 Building main branch with production mode' && vite build --mode production",
		"build:staging": "echo '🚀 Building staging branch with staging mode' && vite build --mode staging",
		"build:dev": "echo '🚀 Building dev branch with development mode' && vite build --mode development",
		"preview": "vite preview"
	},
	"engines": {
		"node": ">=20"
	},
	"dependencies": {
		"react": "^18.2.0",
		"react-dom": "^18.2.0"
	},
	"devDependencies": {
		"@types/react": "^18.2.43",
		"@types/react-dom": "^18.2.17",
		"@vitejs/plugin-react": "^4.2.1",
		"vite": "^6.0.0"
	}
}
